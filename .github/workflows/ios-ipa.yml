name: iOS IPA

# Builds the sideloadable, UNSIGNED Pillion.ipa on a macOS runner and uploads it as an artifact.
# The repo ships no iOS binary, and building it needs macOS + Xcode + the Kotlin/Native toolchain —
# this is the path for anyone (Windows/Linux) who just wants the .ipa to sideload with
# AltStore / SideStore / Sideloadly. See docs/IOS-SIDELOAD.md.

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'iosApp/**'
      - 'composeApp/src/commonMain/**'
      - 'composeApp/src/iosMain/**'
      - 'composeApp/build.gradle.kts'
      - 'gradle/**'
      - '.github/workflows/ios-ipa.yml'

permissions:
  contents: read

jobs:
  ipa:
    # Pinned, not macos-latest: this image's default Xcode is 16.4, which is what Kotlin/Native
    # 2.1.0 was released against. macos-latest now resolves to macos-26 (Xcode 26.4 default) —
    # newer than the K/N toolchain here, and a likely link failure.
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # Kotlin/Native 2.1.0 (see gradle/libs.versions.toml) was released against Xcode 16, so stay
      # on 16.x rather than the image's Xcode 26. Pick 16.4 by name, not by glob: the image carries
      # every 16.x back to 16.0, but only the default (16.4) has its iOS platform fully provisioned
      # — an older one compiles sources fine and then dies on the first storyboard with
      # "iOS 18.0 Platform Not Installed".
      - name: Select Xcode
        run: |
          ls -d /Applications/Xcode*.app || true
          if [ -d /Applications/Xcode_16.4.app ]; then sudo xcode-select -s /Applications/Xcode_16.4.app; fi
          xcode-select -p
          xcodebuild -version

      - name: Install xcodegen
        run: brew install xcodegen

      # The Kotlin/Native toolchain (~1 GB) and the Gradle caches dominate a cold build.
      - name: Cache Kotlin/Native + Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.konan
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-konan-gradle-${{ hashFiles('gradle/libs.versions.toml', 'composeApp/build.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-konan-gradle-

      # project.yml references Signing.xcconfig for every configuration, but it's gitignored (it
      # carries a personal DEVELOPMENT_TEAM). The IPA is built unsigned on purpose — the sideloader
      # re-signs it with the user's own Apple ID — so an empty file is all Xcode needs here.
      - name: Create empty Signing.xcconfig
        run: touch iosApp/Signing.xcconfig

      - name: Build unsigned IPA
        run: |
          chmod +x gradlew iosApp/build-ipa.sh
          cd iosApp && ./build-ipa.sh Release

      # A second, signature-free variant. build-ipa.sh ad-hoc signs inside-out so that a re-signer
      # only has to *replace* an existing seal — but on iOS 26 that still got the extension SIGKILLed
      # 9ms into launch with CODESIGNING "Invalid Page" (the app itself ran fine; only the nested
      # appex's pages failed to match its seal). Strip every signature so the re-signer has to build
      # the seal from scratch instead of replacing one. Which variant a given tool handles correctly
      # is empirical, so ship both and let the installer pick.
      - name: Build signature-free variant
        run: |
          cd iosApp
          rm -rf nosign && mkdir nosign && cd nosign
          unzip -q ../Pillion.ipa
          find Payload -name '*.appex' -print0 | xargs -0 -I{} codesign --remove-signature {}
          codesign --remove-signature Payload/iosApp.app
          rm -rf Payload/iosApp.app/_CodeSignature
          rm -rf Payload/iosApp.app/PlugIns/*.appex/_CodeSignature
          zip -qry ../Pillion-nosign.ipa Payload
          cd ..
          ls -lh Pillion*.ipa

      - uses: actions/upload-artifact@v4
        with:
          name: Pillion-ipa
          path: |
            iosApp/Pillion.ipa
            iosApp/Pillion-nosign.ipa
          if-no-files-found: error
          retention-days: 30
